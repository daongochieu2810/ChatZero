Check `package.json` for scripts to run the nest server
Currently, REST APIs server and web sockets are on the same port. Configure `*.gateway.ts` files under `/gateway` to change port number for a socket